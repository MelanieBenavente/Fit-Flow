<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- Forma personalizada -->
    <item>
        <shape android:shape="rectangle" >
            <solid android:color="@color/home_light_purple" />
            <corners android:radius="4dp" />
            <size
                android:width="55dp"
                android:height="45dp" />
        </shape>
    </item>

    <!-- Vector -->
    <item android:drawable="@drawable/svg_less_line"
        android:gravity="center" />
</layer-list>